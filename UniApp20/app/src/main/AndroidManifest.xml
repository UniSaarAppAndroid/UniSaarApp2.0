<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uniapp_20">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".internet.AppController"
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".NoInternetConnectionActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".SBActivities.EventListActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SBActivities.AboutActivity"
            android:label="Über uns"/>
        <activity
            android:name=".SBActivities.MapFilterActivity"
            android:label="Setting"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".SBActivities.MapsActivity"
            android:label="Map Activity" />
        <activity
            android:name=".SBActivities.DirectoryActivity"
            android:label="Directory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SBActivities.EventActivity"
            android:label="Event"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SBActivities.NewsActivity"
            android:label="News"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SBActivities.EventsSettingActivity"
            android:label="Setting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SBActivities.MensaIngredientActivity"
            android:label="Menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SBActivities.NewsFeedSettingActivity"
            android:label="Settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PrivacyPolicyActivity" />
        <activity
            android:name=".SetupActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SBActivities.MainActivity"
            android:label="News + Events"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>